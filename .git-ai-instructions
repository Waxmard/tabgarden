User POV: me, using the extension in Chrome — the product is what the popup and shortcuts do to my tabs.
"User-facing" = a change in tab grouping, clear-down behavior, AI vs. site fallback, or the popup UI.

- Changing the AI prompt text to improve grouping results → fix
- Improving the group-by-site fallback → fix
