pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,json}"
      run: npm run check:staged -- {staged_files}
      stage_fixed: true
    line-limit:
      glob: "*.{js,html,css}"
      run: bash scripts/check-line-limit.sh {staged_files}
